<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Login{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        {{ encore_entry_script_tags('animation') }}
    {% endblock %}
</head>
<body>
<style>
    html, body{
        height: 100%;
        margin: 0;
        overflow:hidden;
        color: rgb(17, 17, 17);
    }

    label {
        color: #A0AEC0;
    }

    b {
        font-size: 40px;
    }

    .loginContainer {

    }
    .imga {
        width: 414px;
        height: 414px;
        margin-top: 120px;
        margin-left: 25%;
        border-radius: 100px;
        z-index:99999999;
    }
    .hh1 {
        font-size: 40px;
        font-weight: 900;
    }
    .textlogin {
        font-style: italic;
        margin-top: 300px;
        color: #b3ada1;
        margin-left: 500px;
    }
    .logoimg {
        width: 385px;
    }
    .regbtn {
        color: #b3ada1;
        b
    }
</style>
<div class="row logIn">
    <div class="loginpage col">
        <div class="loginContainer">
            <img class="logoimg" src="{{ asset('assets/img/1.png') }}" alt="">
        </div>
        <div class="loginImage">
            <img class="imga" src="{{ asset('assets/img/photo_2022-08-04_11-05-29.jpg') }}" alt="Avatar">
        </div>
    </div>
    <div class="loginpageright col-8">
        <form class="loginform" method="post">
            {% if app.user %}
                <div class="mb-3">
                    Ты уже зашел как {{ app.user.username }}, <a href="{{ path('app_logout') }}">Выйти</a>
                </div>
            {% endif %}
            <div class="text">
                <p class="loginAnimation">
                    <span class="h3 mb-3 hh1 word ">*Тут_крутая_анимация*</span>
                    <span class="h3 mb-3 hh1 word ">Ну,_привет....</span>
                    <span class="h3 mb-3 hh1 word ">Войдешь?</span>
                    <span class="h3 mb-3 hh1 word ">Надо_вначале_зарегаться</span>
                    <span class="h3 mb-3 hh1 word ">Ты_еще_тут?..</span>
                </p>
            </div>

            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            <label class="loginLabelFirst mt-5" for="inputEmail">Тут вроде емейл надо</label>
            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control inputLogin" autocomplete="email" required>
            <label class="mt-5" for="inputPassword">А тут пароль</label>
            <input type="password" name="password" id="inputPassword" class="form-control inputLogin" autocomplete="current-password" required>

            <input type="hidden" name="csrf_token"
                   value="{{ csrf_token('authenticate') }}"
            >

            <div class="row btnsforlogin">
                <button class="col-2 btn btn-lg btn-primar loginBtny mt-5" type="submit">
                    Войти
                </button>
                <div class="col-2"></div>
                <a href="/register" class="col-2 btn btn-lg btn-primar loginBtny mt-5">Зарегаться</a>
            </div>
        </form>

        <div class="row textlogin">
            <p class="">Та что? Нормальный дизайн, типо "минимализм"</p>
            <p class="">Я не фронтенд вообще-то, ясно?</p>
        </div>
    </div>
</div>

</body>
</html>
